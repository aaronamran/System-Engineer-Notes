# Common Linux Commands

- To find a specific file by its name
  ```
  find / -name "FILENAME" 2>/dev/null
  ```
  To find files larger than given size (example 1G)
  ```
  find / -size +1G
  ```

- 
